<?php

return [
    /*
    |--------------------------------------------------------------------------
    | توکن ربات بله
    |--------------------------------------------------------------------------
    |
    | توکن ربات بله که از @BotFather دریافت کرده‌اید.
    |
    */
    'token' => env('BALE_BOT_TOKEN', ''),

    /*
    |--------------------------------------------------------------------------
    | شناسه چت پیش‌فرض
    |--------------------------------------------------------------------------
    |
    | شناسه چت پیش‌فرض برای ارسال پیام‌ها.
    |
    */
    'default_chat_id' => env('BALE_DEFAULT_CHAT_ID'),

    /*
    |--------------------------------------------------------------------------
    | تنظیمات HTTP
    |--------------------------------------------------------------------------
    */
    'options' => [
        'timeout' => (int) env('BALE_TIMEOUT', 30),
        'connect_timeout' => (int) env('BALE_CONNECT_TIMEOUT', 10),
        'base_url' => env('BALE_API_URL', 'https://tapi.bale.ai'),
    ],

    /*
    |--------------------------------------------------------------------------
    | تنظیمات نوتیفیکیشن
    |--------------------------------------------------------------------------
    */
    'notifications' => [
        'errors' => [
            'enabled' => env('BALE_NOTIFY_ERRORS', false),
            'chat_id' => env('BALE_ERROR_CHAT_ID'),
        ],
        'admin_login' => [
            'enabled' => env('BALE_NOTIFY_ADMIN_LOGIN', false),
            'chat_id' => env('BALE_ADMIN_CHAT_ID'),
        ],
        'daily_report' => [
            'enabled' => env('BALE_DAILY_REPORT', false),
            'chat_id' => env('BALE_REPORT_CHAT_ID'),
            'time' => env('BALE_REPORT_TIME', '23:59'),
        ],
    ],
];
